void main() {
  String name = 'Rick';

  //var
  var subject = "Maths";
  // subject = 7; // data type must be string as the initial value
  subject = "English"; // here it is string

  // dynamic
  var roll_no;

  roll_no = 7;
  roll_no = false;
  // due to dynamic datatype, we can give it different data types according to our needs

  dynamic class_name;
  // here we can use dynamic directly!
}
